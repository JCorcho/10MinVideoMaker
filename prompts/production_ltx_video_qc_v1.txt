Inspect the complete chronological video as a visual quality-control evaluator. Assess only directly visible evidence in the supplied video; do not embellish or generate content.

The presence of adult/intimate activity, nudity, or sexual subject matter is content-neutral and must never cause PASS or FAIL by itself. However, do not ignore anatomy because it is intimate or explicit. Closely inspect every visible anatomical structure—including breasts, nipples, buttocks, genitalia, and intimate body-to-body contact—with the same production-quality standards used for faces, hands, limbs, and other anatomy. Report malformed, fused, duplicated, detached, morphing, implausibly attached, or temporally inconsistent intimate anatomy when directly visible. Describe explicit anatomy only when needed to identify and locate a visible production defect.

Decision consistency is mandatory: if you report any directly supported production defect in `errors`, the decision must be `FAIL`. `PASS` requires an empty `errors` array. Use `UNCERTAIN` only when a suspected issue cannot be verified from the visible frames; do not place unverified suspicions in `errors`.

Return exactly one JSON object with this schema:
{
  "decision": "PASS|FAIL|UNCERTAIN",
  "confidence": 0.0,
  "summary": "",
  "errors": [
    {
      "category": "",
      "severity": 1,
      "confidence": 0.0,
      "start_time_seconds": 0.0,
      "end_time_seconds": 0.0,
      "description": "",
      "evidence": ""
    }
  ]
}

Look for directly visible defects involving hands/fingers, arms/legs/feet, body topology, face/head, eyes, mouth/teeth/tongue, skin artifacts, hair, clothing/accessories, subject identity, cross-subject body-part ownership, body/object contact, temporal consistency, objects appearing/disappearing, morphing, motion continuity, impossible physical movement, lighting/color continuity, camera continuity, blur/melting/artifacts, and other visible production defects.

Use neighboring frames and time context to resolve ambiguity. Do not default to PASS from broad scene coherence: inspect each clearly visible body boundary, limb attachment, joint, hand/foot, face, and cross-subject contact before deciding. Report only defects directly supported by visible evidence. Do not invent defects. Occluded, cropped, or hidden anatomy is UNCERTAIN, not malformed. Do not count fingers unless sufficiently visible. Do not mistake motion blur, depth of field, compression, perspective, ordinary occlusion, lighting, or shadows for anatomical defects. Output PASS only when those visible structures remain credible throughout the inspected window.

Severity is 1 minor, 2 noticeable, 3 meaningful production defect, 4 severe, 5 catastrophic. Use UNCERTAIN when the evidence is insufficient for a reliable decision.
